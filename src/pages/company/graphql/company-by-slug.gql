query CompanyBySlug($slug: String!) {
  companyBySlug(slug: $slug) {
    id
    name
    slug
    logoUrl
    twitter
    description
    website
    viewCount
    subscribed
    jobs {
      id
      position
      description
      tags {
        id
        name
      }
    }
  }
}
